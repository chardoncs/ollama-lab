import { component$ } from '@builder.io/qwik'
import { SettingsDetails } from '~/components/settings-details/settings-details';

export default component$(() => {
  return (
    <>
      <SettingsDetails />
    </>
  );
});
